services:
  - type: web
    name: video-to-gif-api
    env: node
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      npm install
    startCommand: node server.js
    plan: free
